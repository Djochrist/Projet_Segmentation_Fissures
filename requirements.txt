# ============================================================
# Dépendances — Projet Segmentation de Fissures
# Compatible Google Colab (GPU T4, A100) et environnement local
# ============================================================

# ─── Vision par ordinateur & Deep Learning ───────────────────
ultralytics>=8.3.0           # YOLOv11 Segmentation
torch>=2.1.0                 # PyTorch (CPU ou CUDA selon l'env)
torchvision>=0.16.0
torchaudio>=2.1.0

# Detectron2 doit être installé séparément selon la version CUDA :
# Sur Colab (CUDA 11.8) :
#   pip install 'git+https://github.com/facebookresearch/detectron2.git'
# Sur Colab (CUDA 12.x) :
#   pip install detectron2 -f https://dl.fbaipublicfiles.com/detectron2/wheels/cu121/torch2.1/index.html

# ─── Traitement d'image ───────────────────────────────────────
opencv-python-headless>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0         # Squelettisation avancée (alternative à cv2)
imageio>=2.31.0

# ─── Calcul scientifique ─────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ─── Visualisation ───────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ─── Outils ML ───────────────────────────────────────────────
pycocotools>=2.0.6           # Évaluation COCO (mAP)
albumentations>=1.3.0        # Augmentation avancée (optionnel)
tensorboard>=2.14.0          # Logs TensorBoard
tqdm>=4.66.0

# ─── Configuration & sérialisation ───────────────────────────
pyyaml>=6.0.1
omegaconf>=2.3.0

# ─── Utilitaires ─────────────────────────────────────────────
pathlib2>=2.3.7; python_version < '3.4'
requests>=2.31.0
gdown>=5.1.0                 # Téléchargement depuis Google Drive
numpy>=1.24.0
albumentations>=1.3.0
gdown>=5.1.0
imageio>=2.31.0
matplotlib>=3.7.0
numpy>=1.24.0
omegaconf>=2.3.0
opencv-python-headless>=4.8.0
pandas>=2.0.0
Pillow>=10.0.0
pycocotools>=2.0.6
pyyaml>=6.0.1
requests>=2.31.0
scikit-image>=0.21.0
scipy>=1.11.0
seaborn>=0.12.0
tensorboard>=2.14.0
torch>=2.1.0
torchaudio>=2.1.0
torchvision>=0.16.0
tqdm>=4.66.0
ultralytics>=8.3.0
albumentations
gdown
imageio
matplotlib
omegaconf
opencv-python-headless
pandas
Pillow
pycocotools
pyyaml
requests
scikit-image
scipy
seaborn
tensorboard
torch
torchvision
tqdm
ultralytics
